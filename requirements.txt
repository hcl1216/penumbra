# Penumbra -- minimal Phase-0 dependencies. NOT YET INSTALLED.
# Phase 0 is CPU-only: load/QC, split-half floor, ridge/GBM baselines, leakage
# controls, headroom decomposition. No neural imputer in Phase 0 (see CLAUDE.md).
#
# License check (all permissive; none copyleft-restrictive for use-as-dependency):
#   numpy          BSD-3-Clause      OK
#   scipy          BSD-3-Clause      OK
#   pandas         BSD-3-Clause      OK
#   scikit-learn   BSD-3-Clause      OK   (ridge baselines, metrics, CV)
#   anndata        BSD-3-Clause      OK   (single-cell container)
#   scanpy         BSD-3-Clause      OK   (QC, spatial neighbours for kNN baseline)
#   statsmodels    BSD-3-Clause      OK   (regress-out / bootstrap stats, Gate C)
#   lightgbm       MIT               OK   (per-gene gradient boosting; GBM candidate map)
#   matplotlib     Matplotlib (BSD-style/PSF-based)  OK   (figures)
#   tqdm           MPL-2.0 + MIT     FLAG: MPL-2.0 is weak (file-level) copyleft.
#                                    Fine as an unmodified dependency; obligations only
#                                    trigger if we modify tqdm's own source. Drop-in
#                                    removable if a license-clean build is ever required.
#
# Deferred to later phases (do not add until needed):
#   pyreadr (Gate E: read METABRIC-IMC .rds) -- pyreadr is MIT but vendors librdata;
#           license-check at point of use.

numpy>=1.24
scipy>=1.10
pandas>=2.0
scikit-learn>=1.3
anndata>=0.10
scanpy>=1.10
statsmodels>=0.14
lightgbm>=4.0
matplotlib>=3.7
tqdm>=4.65
